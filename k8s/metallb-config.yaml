# Configuración compatible con MetalLB v0.9.6 (addon de minikube)
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: local-pool
      protocol: layer2
      addresses:
      - 192.168.191.100-192.168.191.110